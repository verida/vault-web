/**
 * Default fallback string used to display when a value is empty, null, or undefined.
 * This provides consistent empty state handling across the application.
 */
export const EMPTY_VALUE_FALLBACK = "-"
